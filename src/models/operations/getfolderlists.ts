/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { z } from "zod";

/**
 * Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed
 */
export enum GetFolderListsQueryParamSort {
    Asc = "asc",
    Desc = "desc",
}

export type GetFolderListsRequest = {
    /**
     * Id of the folder
     */
    folderId: number;
    /**
     * Number of documents per page
     */
    limit?: number | undefined;
    /**
     * Index of the first document of the page
     */
    offset?: number | undefined;
    /**
     * Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed
     */
    sort?: GetFolderListsQueryParamSort | undefined;
};

export type GetFolderListsResponse = {
    /**
     * HTTP response content type for this operation
     */
    contentType: string;
    /**
     * HTTP response status code for this operation
     */
    statusCode: number;
    /**
     * Raw HTTP response; suitable for custom response parsing
     */
    rawResponse: Response;
    /**
     * Folder's Lists details
     */
    getFolderLists?: components.GetFolderLists | undefined;
};

/** @internal */
export const GetFolderListsQueryParamSort$ = z.nativeEnum(GetFolderListsQueryParamSort);

/** @internal */
export namespace GetFolderListsRequest$ {
    export type Inbound = {
        folderId: number;
        limit?: number | undefined;
        offset?: number | undefined;
        sort?: GetFolderListsQueryParamSort | undefined;
    };

    export const inboundSchema: z.ZodType<GetFolderListsRequest, z.ZodTypeDef, Inbound> = z
        .object({
            folderId: z.number().int(),
            limit: z.number().int().default(10),
            offset: z.number().int().default(0),
            sort: GetFolderListsQueryParamSort$.default(GetFolderListsQueryParamSort.Desc),
        })
        .transform((v) => {
            return {
                folderId: v.folderId,
                ...(v.limit === undefined ? null : { limit: v.limit }),
                ...(v.offset === undefined ? null : { offset: v.offset }),
                ...(v.sort === undefined ? null : { sort: v.sort }),
            };
        });

    export type Outbound = {
        folderId: number;
        limit: number;
        offset: number;
        sort: GetFolderListsQueryParamSort;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, GetFolderListsRequest> = z
        .object({
            folderId: z.number().int(),
            limit: z.number().int().default(10),
            offset: z.number().int().default(0),
            sort: GetFolderListsQueryParamSort$.default(GetFolderListsQueryParamSort.Desc),
        })
        .transform((v) => {
            return {
                folderId: v.folderId,
                limit: v.limit,
                offset: v.offset,
                sort: v.sort,
            };
        });
}

/** @internal */
export namespace GetFolderListsResponse$ {
    export type Inbound = {
        ContentType: string;
        StatusCode: number;
        RawResponse: Response;
        getFolderLists?: components.GetFolderLists$.Inbound | undefined;
    };

    export const inboundSchema: z.ZodType<GetFolderListsResponse, z.ZodTypeDef, Inbound> = z
        .object({
            ContentType: z.string(),
            StatusCode: z.number().int(),
            RawResponse: z.instanceof(Response),
            getFolderLists: z.lazy(() => components.GetFolderLists$.inboundSchema).optional(),
        })
        .transform((v) => {
            return {
                contentType: v.ContentType,
                statusCode: v.StatusCode,
                rawResponse: v.RawResponse,
                ...(v.getFolderLists === undefined ? null : { getFolderLists: v.getFolderLists }),
            };
        });

    export type Outbound = {
        ContentType: string;
        StatusCode: number;
        RawResponse: never;
        getFolderLists?: components.GetFolderLists$.Outbound | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, GetFolderListsResponse> = z
        .object({
            contentType: z.string(),
            statusCode: z.number().int(),
            rawResponse: z.instanceof(Response).transform(() => {
                throw new Error("Response cannot be serialized");
            }),
            getFolderLists: z.lazy(() => components.GetFolderLists$.outboundSchema).optional(),
        })
        .transform((v) => {
            return {
                ContentType: v.contentType,
                StatusCode: v.statusCode,
                RawResponse: v.rawResponse,
                ...(v.getFolderLists === undefined ? null : { getFolderLists: v.getFolderLists }),
            };
        });
}
