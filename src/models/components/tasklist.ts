/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { Task, Task$ } from "./task";
import { z } from "zod";

/**
 * List of tasks
 */
export type TaskList = {
    /**
     * List of tasks
     */
    items?: Array<Task> | undefined;
};

/** @internal */
export namespace TaskList$ {
    export type Inbound = {
        items?: Array<Task$.Inbound> | undefined;
    };

    export const inboundSchema: z.ZodType<TaskList, z.ZodTypeDef, Inbound> = z
        .object({
            items: z.array(Task$.inboundSchema).optional(),
        })
        .transform((v) => {
            return {
                ...(v.items === undefined ? null : { items: v.items }),
            };
        });

    export type Outbound = {
        items?: Array<Task$.Outbound> | undefined;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, TaskList> = z
        .object({
            items: z.array(Task$.outboundSchema).optional(),
        })
        .transform((v) => {
            return {
                ...(v.items === undefined ? null : { items: v.items }),
            };
        });
}
