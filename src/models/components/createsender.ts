/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../internal/utils";
import { Expose, Type } from "class-transformer";

export class Ips extends SpeakeasyBase {
    /**
     * Domain of the IP
     */
    @SpeakeasyMetadata()
    @Expose({ name: "domain" })
    domain: string;

    /**
     * Dedicated IP available in your account
     */
    @SpeakeasyMetadata()
    @Expose({ name: "ip" })
    ip: string;

    /**
     * Weight to apply to the IP. Sum of all IP weights must be 100. Should be passed for either ALL or NONE of the IPs. If it's not passed, the sending will be equally balanced on all IPs.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "weight" })
    weight?: number;
}

export class CreateSender extends SpeakeasyBase {
    /**
     * From email to use for the sender. A verification email will be sent to this address.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "email" })
    email: string;

    /**
     * **Mandatory in case of dedicated IP**. IPs to associate to the sender
     *
     * @remarks
     *
     */
    @SpeakeasyMetadata({ elemType: Ips })
    @Expose({ name: "ips" })
    @Type(() => Ips)
    ips?: Ips[];

    /**
     * From Name to use for the sender
     */
    @SpeakeasyMetadata()
    @Expose({ name: "name" })
    name: string;
}
